{{#paper-card}}
  {{#paper-card-content}}
    <div class="order-card_header">
      {{paper-icon icon="grain"}}
      <span class="spacer"/>
      <h2>{{model.name}}</h2>
    </div>
    {{paper-divider}}
      <div>
        Some data
      </div>
  {{/paper-card-content}}
{{/paper-card}}